# Word Counter (Python)

This project reads a text file and counts how many times each word appears.

## Features
- Removes punctuation
- Ignores common words (the, is, etc.)
- Sorts results by frequency
- Outputs results to a file

## How to run

```bash
python3 word_counter.py
