# Word Counter (Python)

This is a simple Python program that reads a text file and counts how many times each word appears.

## Features
- Reads text from a file
- Ignores punctuation
- Counts word frequency
- Sorts results (most common first)
- Outputs results to a file

## How to run

```bash
python3 word_counter.py
